/*class TestingClass implements TestingInterface {
public boolean basic_method_testing(boolean arg1, boolean arg2) {
    return arg1 && arg2;
}
private int unicode_conversion_testing(char arg1) {
    int ret = arg1;
return ret;
}
public int letter_unicode_sum_independent(String arg1) {
    int sum = 0;
    for (int i = 0; i < arg1.length(); ++i) {
        sum += (int)arg1.charAt(i);
    }
    return sum;
}
public int letter_unicode_sum_dependent(String arg1) {
    int sum = 0;
    for (int i = 0; i < arg1.length(); ++i) {
        sum += unicode_conversion_testing(arg1.charAt(i));
    }
    return sum;
}
}

*/
public class Main {
    public static void main(String[] args) {
        /*TestingClass TestingClassObjectInstance = new TestingClass();
        Scanner input = new Scanner(System.in);
        System.out.println("Input two booleans, full lowercase.");
        System.out.println(TestingClassObjectInstance.basic_method_testing(Boolean.parseBoolean(input.next()), Boolean.parseBoolean(input.next())));
        System.out.println("Input a string.");
        System.out.println(TestingClassObjectInstance.letter_unicode_sum_dependent(input.nextLine()));
        input.close();*/

        
    }
}